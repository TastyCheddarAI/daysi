# packages/contracts

This directory will contain versioned API and event contracts shared across web, API, and workers.

See `docs/execution-api-contract-backlog.md` for the initial contract backlog.
