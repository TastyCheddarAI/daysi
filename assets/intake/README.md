# Intake

Drop raw files into one of these folders:

- `brand/`
- `marketing/`
- `before-after/`

Use the manifest templates in `../manifests/` to describe the files.

Do not rename files with personal names or vague labels like `final-final.jpg`.
